# Research ownership matrix — 2026-09-14 JST

Coordination-only. Production is untouched.

Purpose: prevent parallel chats/workers from duplicating research or invalidating locked evidence.

| lane / branch | owns | must not do | current status |
|---|---|---|---|
| `research/tvfree-canonical-batch02` | V20 canonical temporal/PIT contracts, prospective shadow admission, provenance, immutable resolution receipts, event representation hardening | do not retune Core/Precision from opened returns; do not duplicate Consensus restored-data work | ACTIVE |
| `research/consensus-atr-regime-gate` | promotion-grade clean PIT daily/raw1H restoration, delisted/restored identity continuity, survivorship repair, V47 clean features/model path | do not use V47S survivor-shadow as promotion evidence; do not duplicate Core historical candle exploration | ACTIVE |
| `research/tentei-cloud-mtf` | architecture role mapping, benchmark-role consumer contracts, fixed historical diagnostics, non-overlap coordination | do not build competing V20 event detector; do not build competing PIT/restored data layer; stop adjacent candle-rule enumeration | ACTIVE / coordination-first |
| current production/main | existing live Stable/Sniper/Mega/watchlist/scoring | no research experiment may mutate without explicit approval | PROTECTED |

## Frozen responsibilities on tentei-cloud-mtf

### Allowed
- consume frozen upstream artifacts after their acceptance gates pass;
- classify whether upstream evidence fills Stable/Sniper/Mega product roles;
- common-window comparisons after each lane is legitimately qualified;
- reproducibility / lineage guards that do not open new outcomes;
- update coordination snapshots when newer evidence supersedes older conclusions.

### Closed / do not repeat
- fixed Core threshold pruning;
- Core market-regime veto sweep;
- Core peer-lag sweep;
- direct daily Mega40 semantic clone;
- generic 1H stacking into rejected learned Monster;
- simple raw1H Precision candle/reclaim enumeration;
- blind Core+Monster union;
- data restoration/provider fallback work already owned by Consensus;
- V20 shadow/provenance/event-state work already owned by canonical-batch02.

## Handoff triggers

### Trigger A — clean V47 promotion-grade feature receipt exists
Then tentei-cloud-mtf may:
1. verify receipt eligibility only;
2. wait for upstream selector policy freeze + legitimate locked H2 open;
3. apply `PRECISION_V47_CONSUMER_CONTRACT_20260914.md` without retraining a competing model.

### Trigger B — forward-qualified Monster/event lane exists
Then tentei-cloud-mtf may:
1. run common-window deduplicated role comparison;
2. test Stable-like headline/tail coverage;
3. test whether same lane also covers Mega5;
4. keep Core/Precision classifications independent.

### Trigger C — frozen canonical event stream suitable for long horizon exists
Then a long-horizon consumer may be evaluated against Mega40 role.
Do not reconstruct another event detector here.

## Conflict rule

If two branches appear to own the same new task:
1. preserve the earlier preregistered/locked experiment;
2. use the other branch only as consumer/replication;
3. do not open a second validation set just to choose between implementations;
4. document the ownership resolution before continuing.

## Current highest-value bottleneck

The dominant bottleneck is upstream clean prospective evidence, especially for the positive-tail Monster role.

More retrospective hand-written rules on tentei-cloud-mtf have lower research value and higher overfitting risk.
