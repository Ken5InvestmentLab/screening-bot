# Weak+Early Beta Guidance

- Keep this beta isolated from `screener.js`, `index.js`, `current_logic*.json`, the existing Mega report/gate, production Discord channels, and Google Sheets.
- Preserve the five frozen selector IDs, fixed gates, causal monthly timing, Tail threshold, rank direction, tie-break, and no-cooldown contract. Public names are `Silence`, `Dive`, `Shadow`, `Fusion`, and `Balance`; changing a display name must not change its internal identity.
- Combined results must show both allocations: 100 shares per matched condition (`all_conditions_stacked`) and 100 shares per signal-date/symbol (`all_conditions_unique`). Do not describe overlapping selectors as independent evidence.
- Brand the standalone report as `天底極致 -Cloud-`. Use `確定取引数` / `未確定取引数` instead of internal labels such as `n` or `unit`, keep current-system comparisons out of public HTML, and retain the collapsible monthly metrics table.
- Keep the total report, five mode reports, and guide page as separate generated pages. Every performance page must retain a static asset-growth chart based on required capital plus realized 100-share P/L, and every detection table must remain searchable by normalized symbol code, company name, inclusive signal-date range, and mode.
- Keep beta report navigation and beta JavaScript asset URLs relative so the generated pages work both under the dedicated Worker and from a local `reports/` server. Use the dedicated `weak-early-beta-interactions.js` and `weak-early-beta-theme-init.js`; do not point beta pages at the Mega report scripts.
- Keep the latest 20 detection rows visible initially and reveal 20 more per action. Performance/monthly tables that can grow beyond 20 rows use the same progressive disclosure. Code/name search, inclusive signal-date range filters, result counts, clear, collapse, and per-device theme persistence are required behavior.
- The free beta pages may show mature rows, but every row whose fifth-session close is still unavailable must omit symbol, company name, entry price, chart URL, and fundamental URL from the HTML itself and show the purchase route instead. Role holders continue to receive the full page through the separate beta gate.
- Resolve historical company names through the pinned `state/company_names_202608.csv`; never render missing values as the literal string `nan`.
- Historical canonical rows remain `not_requested_historical`; only forward causal detections enter the fundamental queue. Fundamental worker state must stay under `weak_early_beta/fundamental_worker/` and must never reuse production Premium state.
- Preserve the daily order: same-day OHLCV freshness gate, detection, causal JPX tradability gate, isolated Luna xhigh fundamental analysis and receipt import, HTML generation and beta-only Worker deploy, then signal and analytics-completion Embeds last. A zero-detection business day still sends a zero-result Embed followed by the analytics-completion Embed. The 07:30 fifth-session reminder is idempotent and uses the same beta signal channel. Both runners must stop outside `research/weak-early-beta` and skip Japanese bank holidays without modifying state.
- The tradability gate may exclude only rows for which JPX had already published `上場廃止の決定・整理銘柄指定` by the signal date. Do not exclude all supervision-only securities, do not use later outcomes retroactively, fail the daily run if the official JPX snapshot cannot be refreshed, and preserve excluded forward rows in `state/excluded_detections.csv` with source and designation date.
- Signal Embed titles link to the Japanese TradingView chart. The report link is delivered in a separate analytics-completion Embed. Embedded fundamental text must remain HTML-escaped except for safely reconstructed Markdown/plain URLs, and its box color follows the stated material impact.
- Regenerate outputs with `python -m weak_early_beta.cli report`, test with `python -m unittest tests.test_weak_early_beta`, and verify the separate gate with `cd weak-early-beta-gate && npm run check`.
