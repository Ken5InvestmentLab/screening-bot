# Historical fundamental backfill — complete the omitted batch-4 identity

Use **GPT-6 Luna (`gpt-6-luna`) with `xhigh` reasoning**. The preceding batch-4 run returned only three of four requested reports. Analyze only the missing canonical identity `2023-02-08|4586` (メドレックス), cutoff `2023-02-08T16:15:00+09:00`. Copy its exact selector names from `V:\weak_early_beta\fundamental_worker\out\historical_backfill_luna6_batch4_prompt.md`. Do not regenerate the other three reports.

Read all of `W:\premium_worker\AUTOMATION_PROMPT.md`, `W:\premium_worker\FUNDAMENTAL_EXAMPLES.md`, and `C:\Users\ken5\.codex\skills\premium-fundamental-snapshot\references\report_quality.md`. Inspect the 2023-02-07|4586 report in `V:\weak_early_beta\fundamental_worker\out\historical_backfill_candidate_luna6_batch4_raw.json` only as a prior-day research lead. Independently check the issuer IR listing and IRBANK/TDnet/JPX disclosure list through February 8 at 16:15 JST, then open and read the primary body of each disclosure used. Do not include post-cutoff facts, later announcements, future prices/returns, or information from February 9 onward. In particular, do not copy the prior report's audit note about events/listings after February 7 into this report.

Return one JSON object with one `reports` entry for `2023-02-08|4586`: `signalDate`, `symbolCode`, `companyName`, exact `analysisCutoff`, concise `summary`, `materialImpact` as `ラベル：根拠要約`, `selectorNames`, and `fields` as seven `{ "name", "value" }` objects in exact order `材料インパクト`, `事業概要`, `足元材料`, `ファンダ要点`, `注意点`, `開示リンク`, `Sources`. Include nonempty Japanese values, `sourceChecks` with HTTPS `official_ir` and `irbank_or_tdnet` URLs, and `disclosures` with exact Japanese title, exact publication time including `+09:00`, direct HTTPS document URL, and `contentReviewed: true`. Include only body-read primary documents published at or before the cutoff. Use `auditStatus: "pass"` only if all material claims meet these requirements; otherwise use `needs_repair` and state the exact missing proof. No advice, scores, target prices, or hindsight. Historical analyses are never posted to Discord.

Write only the JSON object to `V:\weak_early_beta\fundamental_worker\out\historical_backfill_candidate_luna6_batch4_missing_20230208_raw.json`. Do not change any other file. Return the same JSON in the final response for inspection.
