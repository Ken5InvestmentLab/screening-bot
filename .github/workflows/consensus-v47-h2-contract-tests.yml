name: Consensus V47 H2 Winner Contract Tests

on:
  push:
    branches:
      - research/consensus-atr-regime-gate
    paths:
      - "research/no_tv_v47_h2_winner_validation.py"
      - "research/test_no_tv_v47_h2_winner_validation.py"
      - ".github/workflows/consensus-v47-h2-contract-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install frozen-model dependencies
        run: pip install pandas==2.3.3 numpy==2.5.3 requests==2.34.2 scikit-learn==1.9.1 scipy==1.18.1 pyarrow
      - name: Compile
        run: |
          python -m py_compile             research/no_tv_v47_h2_winner_validation.py             research/test_no_tv_v47_h2_winner_validation.py
      - name: Run contract
        env:
          PYTHONPATH: research
        run: python research/test_no_tv_v47_h2_winner_validation.py
