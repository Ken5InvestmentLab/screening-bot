# Weak+Early historical fundamental execution-path receipt — 2026-09-23 / 03

- Identity: `WEAK_EARLY_HISTORICAL_EXECUTION_PATH_20260923_03`.
- Branch: `research/weak-early-beta`; starting HEAD `a06ee62e`.
- Purpose: diagnose the reported local-read failure and reproduce the successful 2026-09-02/03 Feature (4052) execution path before resuming the historical backfill.
- Verified successful records: the 2026-09-02 report is present in commit `5313bef07958efacf76c9a7dc06d884ada612bd3`; the 2026-09-03 snapshot is preserved by `273b0ccabf5ec4c235b0b0df60d35474392acc67`. Both have seven report fields and signal-day 23:59 JST cutoffs.
- Successful runner at both commits explicitly used `gpt-5.6-luna` with `xhigh`; this is why those runs were Luna 5.6. The runtime receipt for the 2026-09-03 report records one claimed report, CLI exit 0, successful historical post, and import. This is execution evidence, not a claim that GPT-6 was used.
- Successful local-read route: `scripts/windows/weak-early-beta-fundamental-runner.mjs`, configured via ignored `weak_early_beta/fundamental_worker/out/manual_run_config.json`; it launches the pinned Node runtime and Codex CLI by absolute path, sets `CODEX_HOME`, runs from and passes `--cd` to the `weekly_report_gas` root, and reads the exact Unicode OneDrive paths. The captured 2026-09-03 CLI log shows successful reads of `AUTOMATION_PROMPT.md` (441 lines), `FUNDAMENTAL_EXAMPLES.md` (84), `report_quality.md` (87), and the claim (46). The pinned CLI was `codex-cli 0.153.4`; the global `codex` on `PATH` was `0.125.0` and is not the successful route.
- Local-read probe succeeded for the allowlisted 4052 report and Premium instructions through the mounted `V:`/`W:` aliases. The aliases are a path-transport fallback; they were not necessary in the captured successful 2026-09-03 runner log.
- Bounded retry: invoked that pinned 0.153.4 CLI with the batch-3 prompt, the requested `gpt-6-luna`, `xhigh`, ChatGPT login, weekly-repo working directory, and `-o` to the candidate path. It failed before any candidate was written: `Model metadata for gpt-6-luna not found` followed by HTTP 400, `The 'gpt-6-luna' model is not supported when using Codex with a ChatGPT account.` The unrelated global CLI 0.125.0 had also failed earlier while parsing an unsupported cached reasoning value `max`; it is not the successful local-read path and must not be retried.
- Result: no batch-3 candidate JSON, no import, no Discord post, no HTML/manifest/state change, and no fallback to GPT-5.6. The preserved batch-3 prompt targets only `2023-01-06|3133` and `2023-02-02|7037` with the exact detection-time cutoffs.
- Usage at check: 5-hour window 50% used; weekly window 23% used (77% remaining), above the user's 30%-remaining stop threshold.
- 2026 remains SEALED; production/main/live worker/Discord/Sheets are unchanged. Existing user edits were not staged.
- Next action: use a Codex execution route that actually supports GPT-6 Luna for this account, or obtain the user's explicit approval for GPT-5.6 Luna xhigh as a fallback. Do not use a billable API-key path without explicit billing authorization.
